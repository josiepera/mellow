{
  "name": "project_4",
  "version": "1.0.0",
  "description": "final with ux",
  "main": "app.js",
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^4.1.0",
    "express": "^4.16.4",
    "morgan": "^1.9.1",
    "nodemon": "^1.18.6",
    "pg-promise": "^8.5.2",
    "react-router-dom": "^4.3.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm run start",
    "server": "node app.js",
   "start": "node app.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "author": "Josie Peralta",
  "license": "ISC",
  "heroku-postbuild": "npm install && cd client && npm install --only-dev && npm install && npm run build"
}
